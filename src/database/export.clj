(ns database.export
  "These are the various functions for exporting the data from the database")


(defn db->edn
  "Export the data from the database to an EDN file at `file-path`"
  [file-path]
  ())
(defn db->markdown []
  ())
